# Override modules to empty so Ktor doesn't auto-load the production module.
# This lets TestApp call server() manually with test dependencies (stubs, in-memory DB).
ktor {
  application {
    modules = []
  }
}
